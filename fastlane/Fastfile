default_platform(:ios)

platform :ios do
  desc "Build & upload a TestFlight beta with correct Bundle ID and signing"
  lane :beta do
    setup_ci
    # 0️⃣ Bundle ID
    update_app_identifier(
      xcodeproj:  "ios/MyNewApp.xcodeproj",
      plist_path: "MyNewApp/Info.plist",
      app_identifier: "com.occr.productos"
    )

    # 1️⃣ Team ID
    update_project_team(
      path:    "ios/MyNewApp.xcodeproj",
      targets: ["MyNewApp"],
      teamid:  ENV["DEVELOPMENT_TEAM"]
    )

    # 2️⃣ Signing manual para Release
    update_code_signing_settings(
      use_automatic_signing: false,
      path:                  "ios/MyNewApp.xcodeproj",
      team_id:               ENV["DEVELOPMENT_TEAM"],
      targets:               ["MyNewApp"],
      build_configurations:  ["Release"],
      code_sign_identity:    "Apple Distribution",
      profile_name:          "match AppStore com.occr.productos"
    )

    # ➡️ AÑADIMOS AQUÍ
    increment_build_number(
      xcodeproj:   "ios/MyNewApp.xcodeproj",
      build_number: Time.now.strftime("%Y%m%d%H%M")
    )


    # 3️⃣ Match readonly
    api_key = app_store_connect_api_key(
      key_id:      ENV['APP_STORE_CONNECT_API_KEY_KEY_ID'],
      issuer_id:   ENV['APP_STORE_CONNECT_API_KEY_ISSUER_ID'],
      key_content: ENV['APP_STORE_CONNECT_API_KEY_CONTENT'].gsub("\\n", "\n")
    )

    # Regenerar profiles para incluir Apple Pay + Push Notifications capability
    match(
      type: "appstore", 
      api_key: api_key, 
      readonly: false,  # Cambiar a false para regenerar profiles con Apple Pay + Push
      clone_branch_directly: true,
      force_for_new_devices: true,  # Fuerza regeneración
      force_for_new_certificates: true  # 🔥 FORCE: Descargar nuevo profile con Push Notifications
    )

    # 1️⃣ Bundling RN para iOS
    sh(<<-CMD)
      cd #{Dir.pwd} &&
      npx react-native bundle \
        --entry-file index.js \
        --platform ios \
        --dev false \
        --bundle-output ios/main.jsbundle \
        --assets-dest ios/assets
    CMD

    # 2️⃣ Abort si no existe el bundle
    unless File.exist?("ios/main.jsbundle")
      UI.user_error!("❌ No se generó ios/main.jsbundle — abortando deploy.")
    end

    # 🔥 FIREBASE: Copiar GoogleService-Info.plist al bundle antes de compilar
    UI.message("🔥 Copying GoogleService-Info.plist to ensure it's included in bundle...")
    firebase_source = "ios/MyNewApp/GoogleService-Info.plist"
    firebase_dest = "ios/GoogleService-Info.plist"
    
    if File.exist?(firebase_source)
      FileUtils.cp(firebase_source, firebase_dest)
      UI.success("✅ GoogleService-Info.plist copied to: #{firebase_dest}")
    else
      UI.error("❌ GoogleService-Info.plist not found at: #{firebase_source}")
    end

    # 4️⃣ Compilar con Gym
    begin
      build_app(
        workspace:        "ios/MyNewApp.xcworkspace",
        scheme:           "MyNewApp",
        configuration:    "Release",
        clean:            true,
        export_method:    "app-store",
        output_directory: "build",
        output_name:      "MyNewApp",
        export_options: {
          provisioningProfiles: {
            "com.occr.productos" => "match AppStore com.occr.productos"
          }
        }
      )
    rescue => exception
      sh("echo '----- GYM LOG COMPLETO -----'")
      sh("cat ~/Library/Logs/gym/MyNewApp-MyNewApp.log")
      sh("echo '----- ERRORES DE COMPILACIÓN -----'") 
      sh("grep -i 'error\\|failed\\|failure' ~/Library/Logs/gym/MyNewApp-MyNewApp.log || echo 'No se encontraron errores específicos'")
      raise exception
    end

    ipa_path = Actions.lane_context[SharedValues::IPA_OUTPUT_PATH] # => "/Users/.../build/MyNewApp.ipa"
    sh("unzip -l #{ipa_path} | grep -i main.jsbundle")


    # 5️⃣ Subir a TestFlight
    upload_to_testflight(api_key: api_key)
  end
end
